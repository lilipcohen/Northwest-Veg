<h1>Facilities</h1>

{{!-- <section id="facilities">
    <div class="container">
        <div class="row">
            <div class="col-md-4 text-center">
                <div class="panel panel-success panel-facility">
                    <div class="panel-heading">{{#each this.name}}{{/each}} </div>
                </div>
            </div>
        </div>
    </div>
</section> --}}


<div class="container">
    <div class="row">
        <div class="col-sm-7" id="left-side">
            {{log facility}}
            <ul>

                {{#each facility}}
                

                
                    <p>
                        {{id}}. {{name}}
                        
                        {{address}}
                        
                        {{city}}
                        
                        {{state}}
                        
                        {{postalcode}}
                        
                        {{email}}
                        
                        {{description}}



                    </p>

                
                {{/each}}

            </ul>

        </div>
    </div>
</div>